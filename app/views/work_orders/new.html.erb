<%provide(:for," Work Order " + @workorder.display_number + " ")%>
<div class="page-header">
    <h1>Creating Work Order 
    <%=@workorder.display_number %> for <%= @workorder.client.name %>
    </h1>
</div>
<%= render 'form' %>